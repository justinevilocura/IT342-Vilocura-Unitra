<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Unitra"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".features.landing.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".features.auth.LoginActivity" />
        <activity android:name=".features.auth.RegisterActivity" />
        <activity android:name=".features.auth.PendingActivity" />
        <activity android:name=".features.admin.AdminPendingActivity" />
        <activity android:name=".features.dashboard.MainActivity" />
        <activity android:name=".features.community.CommunityActivity" />
        <activity android:name=".features.marketplace.MarketplaceActivity" />
        <activity android:name=".features.marketplace.CreateListingActivity" />
        <activity android:name=".features.profile.ProfileActivity" />
        <activity android:name=".features.bookings.BookingActivity" />
        
    </application>

</manifest>
